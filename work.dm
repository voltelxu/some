1.决策树

2.boosting, bagging, random forests

数据集的结构:(query, document, grade)

排序方法  BM25

优化方法：

1. Pointwise

2. Pairwise

3. Listwise

特征提取：

1. query feature vector

2. document feature vector

3. query-document feature vector

两个链接之间的关系应该可以用来分析爬虫的准确性

不同位置的特征：
1. title, body,abstract, keywords, author text, url, click
2. ensemble method, bagging,random forests, classifier, regression
